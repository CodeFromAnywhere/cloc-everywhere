{
  "name": "cloc-everywhere",
  "type": "module",
  "main": "cloc-everywhere.cli.ts",
  "version": "0.0.4",
  "scripts": {
    "pub": "npm version patch --force && npm publish"
  },
  "devDependencies": {
    "@types/bun": "^1.1.3",
    "@types/node": "^20.12.7"
  },
  "license": "ISC",
  "description": "Adds information comments to all folders",
  "bin": {
    "cloce": "./cloc-everywhere.cli.ts",
    "cloc-everywhere": "./cloc-everywhere.cli.ts"
  },
  "dependencies": {},
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
